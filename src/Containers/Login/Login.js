import React, { Component } from 'react';
import Class from './Login.scss'

class Login extends Component {
  render() {
    return (
      <div className={Class.title}>Triptic Login Page</div>
    );
  }
}

export default Login;
