import React from 'react';
import Classes from './LoginTitle.scss';

const LoginTitle = props => {
    return (
        <div className={Classes.title}>Triptic</div>
      );
}

export default LoginTitle;